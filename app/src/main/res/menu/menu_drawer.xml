<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Encabezado: Nombre e icono de la App -->
    <group android:id="@+id/group_header">
        <item
            android:id="@+id/menu_header"
            android:icon="@android:mipmap/sym_def_app_icon"
            android:title="@string/app_name" />
    </group>

    <!-- Opción: VideoView -->
    <item
        android:id="@+id/menu_video_view"
        android:icon="@android:drawable/ic_menu_slideshow"
        android:title="Video View" />

    <!-- Opción: CalendarView -->
    <item
        android:id="@+id/menu_calendar_view"
        android:icon="@android:drawable/ic_menu_my_calendar"
        android:title="Calendar View" />

    <!-- Opción: Google Maps -->
    <item
        android:id="@+id/menu_google_maps"
        android:icon="@android:drawable/ic_dialog_map"
        android:title="Google Maps" />

    <!-- Opción: Gráfico Lineal -->
    <item
        android:id="@+id/menu_line_chart"
        android:icon="@android:drawable/ic_menu_gallery"
        android:title="Gráfico Lineal" />

</menu>